@model Repository.Models.Movie

@using (Html.BeginForm())
{
    <div>
        @Html.LabelFor(m => m.Title, "Title:")
        @Html.TextBoxFor( m => m.Title)
    </div>

    <div>
        @Html.LabelFor(m => m.Director, "Director:")
        @Html.TextBoxFor(m => m.Director)
    </div>

    <input type="submit" value="Edit" />
}